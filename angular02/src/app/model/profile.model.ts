export class Profile {
    fname: string;
    lname: string;
    profileImage: string;
    status: string;

    /* public constructor(init?: Partial<Profile>) {
        console.log(init);
        
        Object.assign(this, init);
    } */
}